name: learn-github-actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
     - name: Step 1 - define Python version
       run: python --version
     - name: Step 2 - Hello Step
       run: echo "Hello World!"
     - name: Step 2 - Hello Step
       run: echo "Hello World!"
